// =========================
//  Arquivo principal JS
//  Espaço Poliana
// =========================

// Exemplo: mensagem no console confirmando que o JS está funcionando
console.log("script.js carregado com sucesso!");

// Exemplo de código futuro para o menu (se quiser transformar em mobile menu depois)
function abrirMenu() {
    const menu = document.querySelector("nav ul");
    menu.classList.toggle("menu-ativo");
}

// Exemplo para futuramente validar formulários de agendamento/contato
function validarFormulario() {
    // Lógica será adicionada depois
    console.log("Formulário validado!");
}